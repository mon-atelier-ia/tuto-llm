# Runtime
jupyter~=1.1

# Testing (ISO 29119)
pytest~=9.0
pytest-cov~=7.0

# Linting & Formatting (ISO 5055)
ruff~=0.15
mypy~=1.19

# Security (ISO 27001)
pip-audit~=2.10

# Git hooks & Conventional commits (ISO 12207)
pre-commit~=4.5
commitizen~=4.13
